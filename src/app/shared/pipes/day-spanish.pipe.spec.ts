import { DaySpanishPipe } from './day-spanish.pipe';

describe('DaySpanishPipe', () => {
  it('create an instance', () => {
    const pipe = new DaySpanishPipe();
    expect(pipe).toBeTruthy();
  });
});
