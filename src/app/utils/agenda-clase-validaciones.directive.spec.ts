import { AgendaClaseValidacionesDirective } from './agenda-clase-validaciones.directive';

describe('AgendaClaseValidacionesDirective', () => {
  it('should create an instance', () => {
    const directive = new AgendaClaseValidacionesDirective();
    expect(directive).toBeTruthy();
  });
});
