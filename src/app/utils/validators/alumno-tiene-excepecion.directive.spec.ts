import { AlumnoTieneExcepecionDirective } from './alumno-tiene-excepecion.directive';

describe('AlumnoTieneExcepecionDirective', () => {
  it('should create an instance', () => {
    const directive = new AlumnoTieneExcepecionDirective();
    expect(directive).toBeTruthy();
  });
});
