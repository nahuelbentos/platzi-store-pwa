import { AlumnoYaAsignadoDirective } from './alumno-ya-asignado.directive';

describe('AlumnoYaAsignadoDirective', () => {
  it('should create an instance', () => {
    const directive = new AlumnoYaAsignadoDirective();
    expect(directive).toBeTruthy();
  });
});
