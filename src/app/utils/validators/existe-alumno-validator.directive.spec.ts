import { ExisteAlumnoValidatorDirective } from './existe-alumno-validator.directive';

describe('ExisteAlumnoValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExisteAlumnoValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
