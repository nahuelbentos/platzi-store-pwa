import { InstructorYaAsignadoValidatorDirective } from './instructor-ya-asignado-validator.directive';

describe('InstructorYaAsignadoValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new InstructorYaAsignadoValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
