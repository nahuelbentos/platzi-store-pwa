import { LicenciaInstructorValidatorDirective } from './licencia-instructor-validator.directive';

describe('LicenciaInstructorValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LicenciaInstructorValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
